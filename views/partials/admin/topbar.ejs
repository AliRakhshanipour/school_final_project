<div class="bg-white border-b-2 border-gray-200 px-4 py-3 flex justify-between items-center">
    <!-- Header Right Section -->
    <div class="flex flex-grow items-center">
        <!-- Menu Button (Can be removed if not needed) -->
        <button class="text-gray-600 focus:outline-none">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"/>
            </svg>
        </button>
    </div>

    <!-- Header Left Section -->
    <div class="flex items-center">
        <!-- Logout Link -->
        <a href="/logout" class="text-gray-600 hover:text-gray-800">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6-4v8"/>
            </svg>
        </a>
    </div>
</div>

<!-- Breadcrumb Section -->
<div class="bg-gray-200 px-4 py-2">
    <nav class="text-gray-600">
        <ol class="list-none p-0 inline-flex">
            <li class="flex items-center">
                <a href="/admin" class="text-gray-500 hover:text-gray-700" title="پیشخوان">پیشخوان</a>
            </li>
        </ol>
    </nav>
</div>
